import React from "react"
import styled from "styled-components"
const About = () => {
  return <Styled>The About Page</Styled>
}
const Styled = styled.div`
  margin-top: 70px;
`

export default About
